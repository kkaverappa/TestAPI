{
  dataSet: {...} //the original data that was sent to run rules on, 
  dataModel: {...} //the model from RDM that the data was matched to, 
  rules: [...] //the rules that were run on the data, 
  result: [...] || {...} //the result(s) of the rules that were run
}
